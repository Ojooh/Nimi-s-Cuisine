<!-- Mobile side Nav Bar -->
<div class="site-mobile-menu">
    <div class="site-mobile-menu-header">
        <div class="site-mobile-menu-close">
            <span class="icofont-close js-menu-toggle"></span>
        </div>
    </div>
    <div class="site-mobile-menu-body"></div>
</div>
<!-- Mobile side Nav bar End -->

<!-- Navbar top -->
<nav class="site-nav mb-5">
    <div class="container-fluid">
    <%if (lnks && lnks.length> 0) { %>
        <div class="site-navigation text-center">
            <a href="index.html" class="logo menu-absolute m-0">
                <img src="img/logo/logo.png" img-fluid">
            </a>
            <ul class="js-clone-nav d-none d-lg-inline-block site-menu">
            <% for (var l=0; l < lnks.length; l++) { var par = lnks[l].id %>
                <% if (lnks[l].parent==0) { %>
                <li class="<%= (( lnks[l].name == actv) ? 'active' : actv) %> <%= (( lnks[l].children == '1') ? 'has-children' : '') %>">
                    <a href="index.html">
                       <%= lnks[l].name %>

                        <% if (lnks[l].children == '1') { %>
                            <i class="fas fa-angle-down"></i>
                        <% } %>
                    </a>

                    <ul class="dropdown <%= (( lnks[l].children == '1') ? '' : 'deactivated') %>">
                    <% for (var f=0; f < lnks.length; f++) { %>
                        <% if (lnks[f].parent==par) { %>
                            <li><a href="elements.html"><%= lnks[f].name %></a></li>
                        <% } %>
                    <% } %>
                    </ul>
                </li>
                <% } %>
                
            <% } %>
                
            </ul>
            <a href="book-a-table.html" class="btn-book btn btn-primary btn-sm menu-absolute right-40">
                <i class="fas fa-shopping-cart"></i>
            </a>
            <a href="book-a-table.html" class="btn-book btn btn-primary btn-sm menu-absolute">
                Book a meal
            </a>
            <a href="#"
                class="burger ml-auto float-right site-menu-toggle js-menu-toggle d-inline-block d-lg-none light"
                data-toggle="collapse" data-target="#main-navbar">
                <span></span>
            </a>
        </div>
    <% } %>
    </div>
</nav>
<!-- Navbar Top End -->